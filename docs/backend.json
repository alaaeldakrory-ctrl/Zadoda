{
  "entities": {
    "Person": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Person",
      "type": "object",
      "description": "Represents one of the four core users in the Zadoda Scheduler application, with customizable display name and a distinct color theme.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Person entity. Fixed IDs (e.g., 'person1', 'person2', 'person3', 'person4') are used."
        },
        "name": {
          "type": "string",
          "description": "The display name for the person, configurable by the user."
        },
        "color": {
          "type": "string",
          "description": "A hexadecimal color code (e.g., '#RRGGBB') assigned to the person for visual distinction in the calendar."
        }
      },
      "required": [
        "id",
        "name",
        "color"
      ]
    },
    "Memo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Memo",
      "type": "object",
      "description": "A personal note or memo belonging to a specific family member.",
      "properties": {
        "id": {
          "type": "string"
        },
        "personId": {
          "type": "string",
          "description": "The ID of the person this memo belongs to."
        },
        "title": {
          "type": "string"
        },
        "content": {
          "type": "string"
        },
        "createdAt": {
          "type": "number"
        },
        "completed": {
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "personId",
        "title",
        "content",
        "createdAt"
      ]
    },
    "FixedEventTemplate": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FixedEventTemplate",
      "type": "object",
      "description": "A reusable template for frequently scheduled events, allowing users to quickly add pre-defined events to the calendar.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the FixedEventTemplate entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the event template, displayed in the templates library."
        },
        "defaultDurationMinutes": {
          "type": "number",
          "description": "The default duration for an event created from this template, in minutes. Must be a multiple of 15."
        },
        "defaultTime": {
          "type": "string",
          "description": "An optional default time of day (HH:MM) for events created from this template.",
          "format": "time"
        },
        "defaultAssigneePersonId": {
          "type": "string",
          "description": "Optional reference to the Person ID who is typically assigned this event."
        },
        "notes": {
          "type": "string",
          "description": "Optional detailed notes or description for events created from this template."
        },
        "color": {
          "type": "string",
          "description": "Optional hexadecimal color code for events created from this template."
        }
      },
      "required": [
        "id",
        "name",
        "defaultDurationMinutes"
      ]
    },
    "CalendarEventSeries": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CalendarEventSeries",
      "type": "object",
      "description": "Represents a single event or a series of recurring events on the calendar, defining its core properties and recurrence rules.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CalendarEventSeries entity."
        },
        "personId": {
          "type": "string",
          "description": "Reference to the Person ID to whom this event series is assigned."
        },
        "title": {
          "type": "string",
          "description": "The main title or name of the event series."
        },
        "notes": {
          "type": "string",
          "description": "Optional detailed notes or description for the event series."
        },
        "templateId": {
          "type": "string",
          "description": "Optional reference to the FixedEventTemplate ID from which this series was created."
        },
        "startTime": {
          "type": "string",
          "description": "The standard start time of day (HH:MM).",
          "format": "time"
        },
        "endTime": {
          "type": "string",
          "description": "The standard end time of day (HH:MM).",
          "format": "time"
        },
        "startDate": {
          "type": "string",
          "description": "The effective start date (YYYY-MM-DD).",
          "format": "date"
        },
        "frequency": {
          "type": "string",
          "description": "The recurrence frequency. 'NONE' for single events."
        },
        "interval": {
          "type": "number",
          "description": "The interval for recurrence."
        },
        "byWeekday": {
          "type": "array",
          "items": { "type": "string" }
        },
        "untilDate": {
          "type": "string",
          "format": "date"
        },
        "isImportant": {
          "type": "boolean",
          "description": "Flag to highlight the event as important."
        }
      },
      "required": [
        "id",
        "personId",
        "title",
        "startTime",
        "endTime",
        "startDate"
      ]
    },
    "EventOccurrenceOverride": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EventOccurrenceOverride",
      "type": "object",
      "description": "Stores specific overrides for individual occurrences of a CalendarEventSeries.",
      "properties": {
        "id": {
          "type": "string"
        },
        "seriesId": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "format": "date"
        },
        "completed": {
          "type": "boolean"
        },
        "completedAt": {
          "type": "number"
        },
        "isImportant": {
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "seriesId",
        "date",
        "completed"
      ]
    },
    "AppSettings": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AppSettings",
      "type": "object",
      "description": "Stores application-wide configuration settings.",
      "properties": {
        "id": { "type": "string" },
        "dayStartTime": { "type": "string", "format": "time" },
        "dayEndTime": { "type": "string", "format": "time" },
        "language": { "type": "string" }
      },
      "required": [
        "id",
        "dayStartTime",
        "dayEndTime",
        "language"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/appSettings/{settingsId}",
        "definition": {
          "entityName": "AppSettings",
          "schema": { "$ref": "#/backend/entities/AppSettings" },
          "description": "Stores application-wide configuration settings."
        }
      },
      {
        "path": "/people/{personId}",
        "definition": {
          "entityName": "Person",
          "schema": { "$ref": "#/backend/entities/Person" },
          "description": "Defines family members."
        }
      },
      {
        "path": "/memos/{memoId}",
        "definition": {
          "entityName": "Memo",
          "schema": { "$ref": "#/backend/entities/Memo" },
          "description": "Personal memos/tasks."
        }
      },
      {
        "path": "/fixedEventTemplates/{templateId}",
        "definition": {
          "entityName": "FixedEventTemplate",
          "schema": { "$ref": "#/backend/entities/FixedEventTemplate" },
          "description": "Reusable event templates."
        }
      },
      {
        "path": "/calendarEventSeries/{seriesId}",
        "definition": {
          "entityName": "CalendarEventSeries",
          "schema": { "$ref": "#/backend/entities/CalendarEventSeries" },
          "description": "Calendar event series."
        }
      },
      {
        "path": "/calendarEventSeries/{seriesId}/eventOccurrenceOverrides/{overrideId}",
        "definition": {
          "entityName": "EventOccurrenceOverride",
          "schema": { "$ref": "#/backend/entities/EventOccurrenceOverride" },
          "description": "Overrides for specific occurrences."
        }
      }
    ],
    "reasoning": "Standard hierarchical and flat collections for a family application."
  }
}
